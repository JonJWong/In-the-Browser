const Arrow = require('./arrow.js');
const Receptor = require('./arrow.js');

class Game {
  constructor() {
    this.score = 0;
    this.maxScore = gameOpts['numNotes'] * 5;
  }
}

module.exports = Game;