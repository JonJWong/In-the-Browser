const Arrow = require('./arrow.js');

class Receptor extends Arrow {
  constructor(num) {
    this.receptors;
  }

  addReceptors(num) {
    let receptors = [];
    let i = 0;
    while (receptors.length < num) {
      
    }
  }
}

module.exports = Receptor;